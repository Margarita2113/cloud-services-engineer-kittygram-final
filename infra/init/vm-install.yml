ssh_pwauth: no

users:
  - name: user
    sudo: ALL=(ALL) NOPASSWD:ALL
    shell: /bin/bash
    ssh_authorized_keys:
      - "${SSH_KEY}"
  - name: user_test
    sudo: ALL=(ALL) NOPASSWD:ALL
    shell: /bin/bash
    passwd: "ee26b0dd4af7e749aa1a8ee3c10ae9923f618980772e473f8819a5d4940e0db27ac185f8a0e1d5f84f88bc887fd67b143732c304cc5fa9ad8e6f57f50028a8ff"
    lock_passwd: false
    
write_files:
  - content: |
      #!/bin/bash
      apt-get update
      apt-get install -y docker.io docker-compose
      usermod -aG docker user
    path: /root/vm_prep.sh
    permissions: "0740"

runcmd:
  - sleep 120
  - sudo -i
  - /root/vm_prep.sh
